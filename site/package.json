{
  "name": "sthara-marketing-launch",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": "24.x" },
  "scripts": {
    "build": "node build.mjs",
    "export": "node build.mjs && node export.mjs",
    "dev": "node preview.mjs",
    "test": "node --test tests/*.test.mjs",
    "verify": "node verify.mjs"
  }
}
